import React, { Component } from 'react';

export default class IncursionCardTable extends Component {
  // NOTE: 3 high, 3 low, one null?
  // NOTE: Maybe output a list of cards instead.
  constructor(props) {
    super(props)
    this.state = {}
    this.componentDidMount = this.componentDidMount.bind(this);
  }


  render() {
    return (
      <div></div>
    )
  }
}
